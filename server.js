const express = require('express'); // importing express
const mongoose = require('mongoose'); //importing mongoose
const app = express();
const routes = require('./routes'); // './' path to a file 

mongoose.connect('')